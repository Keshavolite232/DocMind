# Core RAG stack
langchain>=0.3.0,<0.4.0
langchain-community>=0.3.0,<0.4.0
langchain-anthropic>=0.2.0,<0.4.0
# LLM
anthropic>=0.34.0

# Embeddings — uses chromadb's built-in ONNX model (no PyTorch, no Rust deps)
# onnxruntime is already a chromadb dependency; listed here to ensure it's explicit
onnxruntime>=1.16.0

# Vector stores
# langchain-chroma 0.2.x requires chromadb>=0.6.0 which has breaking API changes;
# pin to 0.1.x to stay compatible with chromadb 0.5.x
chromadb>=0.5.0,<0.6.0
langchain-chroma>=0.1.0,<0.2.0

# Document loading
pypdf>=4.3.0

# UI
streamlit>=1.38.0

# Dev / testing
pytest>=8.3.0
pytest-mock>=3.14.0
python-dotenv>=1.0.0
