# Core RAG stack
langchain>=0.3.0,<0.4.0
langchain-community>=0.3.0,<0.4.0
langchain-anthropic>=0.2.0
langchain-huggingface>=0.1.0   # replaces langchain-openai for embeddings

# LLM
anthropic>=0.34.0

# Embeddings (runs locally — no API key required)
sentence-transformers>=3.0.0

# Vector stores
chromadb>=0.5.0,<1.0.0
langchain-chroma>=0.1.0

# Document loading
pypdf>=4.3.0

# UI
streamlit>=1.38.0

# Dev / testing
pytest>=8.3.0
pytest-mock>=3.14.0
python-dotenv>=1.0.0
